# Lab API routes

Port `orchestrate/route.ts` and standard lab runner from umbra-pro here when splitting fully.

Source of truth during migration:
- umbra-pro: `src/app/api/lab/orchestrate/route.ts`
- umbra-pro: `src/app/api/lab/route.ts`
